<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_raise_issue"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="in.andonsystem.v2.activity.RaiseIssueActivity2"
    tools:showIn="@layout/activity_raise_issue2">

    <Spinner
        android:id="@+id/ri_team_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8sp"></Spinner>

    <Spinner
        android:id="@+id/ri_buyer_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8sp"></Spinner>

    <Spinner
        android:id="@+id/ri_problem_filter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16sp"></Spinner>

    <EditText
        android:id="@+id/ri_problem_desc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLength="70"
        android:hint="Issue Description"
        android:inputType="text"
        android:gravity="top"
        android:layout_marginBottom="16sp"/>

    <Button
        android:id="@+id/raise_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Raise Issue"
        android:background="@color/tomato"
        android:onClick="raiseIssue"/>

</LinearLayout>
